{
  "name": "@motd-menu/server",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "webpack --mode=production",
    "build:dev": "webpack --mode=development",
    "launch": "node ./dist",
    "start": "npm run build && npm run launch",
    "watch": "concurrently \"webpack --mode=development --watch\" \"nodemon ./dist --watch ./dist\"",
    "wipedb": "ts-node ./wipe-db"
  },
  "dependencies": {
    "@motd-menu/common": "1.0.0"
  }
}
